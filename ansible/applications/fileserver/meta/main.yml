# =========================================================================================== #
# File: '{{ role_path }}/meta/main.yml'
# --- [ Description ] ----------------------------------------------------------------------- #
# Role metadata for fileserver. Description-only (style guide §8): NOT an enforcing argument
# spec. The role's tasks/validate.yml enforces its merged-config guards.
#
# Defaults describe the ratified SMB, four-node cluster, disk-owner, AZ-a file-server role,
# and encrypted share policy. Runtime volume identities and the cluster password are resolved
# only by the playbook. Witness quorum, Storage Replica, and a second file-server role are
# explicitly outside the implemented surface; tasks/validate.yml owns enforcement.
# =========================================================================================== #
---
galaxy_info:
  role_name: 'fileserver'
  namespace: 'nwarila_platform'
  author: 'nwarila-platform'
  description: >-
    Windows Server 2025 file service: SMB and node prerequisites, a four-node WSFC cluster,
    two adopted owner-scoped shared disks, one AZ-a clustered file-server role, and its exact
    encrypted continuously available data share. Workgroup witness quorum, Storage Replica,
    and an AZ-b file-server role are excluded.
  license: 'MIT'
  min_ansible_version: '2.17'
  platforms:
    - name: 'Windows'
      versions:
        - '2025'
  galaxy_tags: ['windows', 'fileserver', 'cluster']

dependencies: []
