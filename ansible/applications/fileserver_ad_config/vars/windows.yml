# =========================================================================================== #
# File: '{{ role_path }}/vars/windows.yml'
# --- [ Description ] ----------------------------------------------------------------------- #
#
# Windows-family overlay merged over fileserver_ad_config_defaults by the v3 loader.
#
# This role acts on a directory through a domain controller, so the defaults are already the
# Windows values and this overlay carries nothing. It exists because the loader resolves an OS
# overlay per family; an empty map is the honest statement that this family needs no override,
# rather than a duplicate of defaults that would drift.
#
# =========================================================================================== #
---
{}
